---
long_title: definition of a discrete uniform random variable and its pmf
short_title: discrete uniform variable
type: item
subtype: definition
keywords:
  - discrete uniform
  - uniform variable
  - finite set
  - pmf
---

A discrete random variable $X$ is **uniform** on a finite set $A\subset S$, written $X\sim \mathrm{Unif(A)}$, when the probability is a fixed constant for outcomes in $A$ and zero for outcomes outside $A$.

Discrete uniform PMF:

$$
P_X(k) \;=\; \begin{cases}\dfrac{1}{|A|} & \text{when }k\in A\\ 0&\text{when }k\notin A\end{cases}
$$
