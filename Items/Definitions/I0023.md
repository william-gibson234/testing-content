---
long_title: definition of events and probabilities determined by a random variable
short_title: random variable events
type: item
subtype: definition
keywords:
  - random variable
  - event
  - preimage
  - probability notation
  - range
---

Random variables determine events.
- Given $a\in\mathbb{R}$, the event "$X=a$" is equal to the set $X^{-1}(a)$
- That is: the set of outcomes mapped to $a$ by $X$
- That is: the event "$X$ took the value $a$"

Such events have probabilities. We write them like this:

$$
P[X=a]\quad \gg\gg \quad P[X^{-1}(a)]
$$

This generalized to events where $X$ lies in some range or set, for example:

$$
P[a\leq X<b],\qquad P\big[X\in \{2,4,5,6,9\}\big]
$$
