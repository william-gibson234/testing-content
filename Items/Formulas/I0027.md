---
long_title: formula for the cdf of a discrete random variable as a sum of pmf values
short_title: cdf from pmf formula
type: item
subtype: formula
keywords:
  - cdf
  - pmf
  - discrete rv
  - sum formula
---

For a discrete RV, the CDF and the PMF can be calculated from each other using formulas.

Given a PMF $P_X(x)$, the CDF is determined by:

$$
F_X(x) = \sum_{k_i\leq x} P_X(k_i)
$$

where $\{k_1,\,k_2,\,\dots\}$ is the set of possible values of $X$.
