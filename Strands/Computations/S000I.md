---
long_title: computing p(at least two heads) via counting, then conditioning on at least one head
short_title: at least 2 heads
type: strand
subtype: computation
keywords:
  - counting
  - cond probability
  - coin flips
linked:
  - I000S
parent_item: I000S
---
Flip a fair coin 4 times and record the outcomes as sequences, like HHTH.

Let $A_{\geq 2}$ be the event that there are at least two heads, and $A_{\geq 1}$ the event that there is at least one heads.

First let’s calculate $P[A_{\geq 2}]$.

Define $A_2$, the event that there were exactly 2 heads, and $A_3$, the event of exactly 3, and $A_4$ the event of exactly 4. These events are exclusive, so:

$$
P[A_{\geq 2}]=P[A_2\cup A_3\cup A_4]\quad \gg\gg \quad P[A_2]+P[A_3]+P[A_4]
$$

Each term on the right can be calculated by counting:

$$
\begin{align*}P[A_2]&= \frac{|A_2|}{2^4}\quad \gg\gg \quad \frac{4\choose2}{16}\quad \gg\gg \quad \frac{6}{16}\\\\ P[A_3] &= \frac{|A_3|}{2^4}\quad \gg\gg \quad \frac{4\choose3}{16}\quad \gg\gg \quad \frac{4}{16}\\\\ P[A_4]&= \frac{|A_4|}{2^4}\quad \gg\gg \quad \frac{4\choose4}{16}\quad \gg\gg \quad \frac{1}{16}\end{align*}
$$

Therefore, $P[A_{\geq 2}]=\frac{11}{16}$.

Now suppose we find out that “at least one heads definitely came up”. (Meaning that we know $A_{\geq 1}$.) For example, our friend is running the experiment and tells us this fact about the outcome.

Now what is our estimate of likelihood of $A_{\geq 2}$?

The formula for conditioning gives:

$$
P[A_{\geq 2}\mid A_{\geq 1}]=\frac{P[A_{\geq 2}\cap A_{\geq 1}]}{P[A_{\geq 1}]}
$$

Now $A_{\geq 2}\cap A_{\geq 1}=A_{\geq 2}$. (Any outcome with at least two heads automatically has at least one heads.) We already found that $P[A_{\geq 2}]=\frac{11}{16}$. To compute $P[A_{\geq 1}]$ we simply *add* the probability $P[A_1]$, which is $\frac{4}{16}$, to get $P[A_{\geq 1}]=\frac{15}{16}$.

Therefore:

$$
P[A_{\geq 2}\mid A_{\geq 1}]=\frac{11/16}{15/16}\quad \gg\gg \quad \colorbox{cyan}{$\frac{11}{15}$}
$$
